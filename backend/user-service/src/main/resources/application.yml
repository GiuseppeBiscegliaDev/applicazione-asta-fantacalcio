server:
  port: 8083

spring:
  application:
    name: USER_SERVICE
  datasource:
    url: jdbc:postgresql://fantacalcio-postgres:5432/fantacalcio
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/