server:
  port: 8080

spring:
  application:
    name: asta-service
  datasource:
    url: jdbc:postgresql://fantacalcio-postgres:5432/fantacalcio
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
    register-with-eureka: true
    fetch-registry: true